// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String @id @default(uuid())
  nome      String
  matricula String
  curso     String

  requests Request[]

  @@unique([matricula])
  @@map("users")
}

model Request {
  id      String @id @default(uuid())
  modelo  String
  cor     String
  tamanho String @db.Char(1)
  user_id String

  user User @relation(fields: [user_id], references: [id])

  @@map("requests")
}
